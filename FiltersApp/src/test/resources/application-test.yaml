server:
  port: ${APP_PORT:8081}
  servlet:
    context-path: /api

spring:
  application:
    name: 'Filters App'
  jpa:
    database: postgresql
    properties:
      hibernate:
        jdbc:
          time_zone: Europe/Tallinn
  datasource:
    driver-class-name: org.postgresql.Driver

